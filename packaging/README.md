# Packaging (Entwürfe / drafts)

- `aur/PKGBUILD` — draft for an AUR `voxtype-git` package (not yet
  published, see issue #9). System-wide install, whisper.cpp as AUR
  dependency, model downloaded on first run via the control center.
- Flatpak/COPR: open — Flatpak needs an architecture decision first
  (sandbox vs. evdev/uinput access).

Publishing requires the maintainer's AUR account; review before pushing.
